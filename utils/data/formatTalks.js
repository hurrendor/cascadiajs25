function formatTalks(text){
    const formatted = `# ${text.title} \n`
    abstract
    const speakerInfo = `## Speaker information \n
    Name: ${text.name}}`
    text.links.forEach((link)=>{
        `(${link.url})[${link.type}]`
    })
    return ``
}